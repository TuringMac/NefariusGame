import { GameService } from "./game.service";

export class Player {
  public name: string;
  public score: number;
  public action: number;

  public setSpy(area: number) {
    //this.gameService.setSpy(area);
  }

  public dropSpy(area: number) {
    //this.gameService.dropSpy(area);
  }

  public getSpies(area: number) {

  }
}
